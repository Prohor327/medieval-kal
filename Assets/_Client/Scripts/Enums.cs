public enum PlayerMovementState
{
    None = 0,
    Idle = 1,
    Walk = 2,
} 

public enum PlayerCombatState
{
    None = 0,
    Idle = 1,
    Move = 2,
    Attack = 3,
    Block = 4,
    Blocking = 5,
    Unblock = 6 
}

public enum SkeletonBigState
{
    None = 0,
    Idle = 1, 
    Walk = 2,
    Attack = 3
}